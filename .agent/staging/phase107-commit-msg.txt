seal: phase 107 - carry-forward close en masse (v0.75.0; V2 list reaches zero)

Closes ALL five V2 carry-forward items accumulated across Phases 101-106
in one cohesive cycle per the operator's `/qor-auto-dev-1` "wrap with
zero pending items remaining" directive.

Five deliverables:
- D-107.1: cooling-period lint flipped WARN-only -> hard fail
  (removed `|| true` from pr-dependency-review.yml; lint exit-1 on
  real violations blocks PR merge; override path remains operative).
- D-107.2: cyclonedx-bom hash-pinning. NEW requirements-sbom.{in,txt}
  (autogenerated, ~392 lines, Razor-exempt per plan's explicit narrow
  exemption block). release.yml SBOM step switched to
  `pip install --require-hashes -r requirements-sbom.txt`.
- D-107.3: range-pin lower-bound coverage. NEW
  parse_pyproject_range_pins(text) extracts lower bounds from `>=` /
  `~=` specifiers; lint applies cooling-period check to those too.
- D-107.4: GH-issue emit. `--emit-issue` flag on
  dep_admit_override_tracker invokes `gh issue create` with rollup
  body for due entries (single issue per invocation, anti-spam).
- D-107.5: CODEOWNERS solo-owner doctrine. New section 6.1 in
  doctrine-governance-enforcement.md documents current state as
  deliberate operational mode with 4 expansion triggers; closes via
  documentation, not scaffold change.

- 13 new behavioral tests; 51/51 pass twice deterministically.
- META_LEDGER #291-#293; Merkle seal 5fd15746...
- Razor exemption operative for requirements-sbom.txt (narrow scope).
- SG-StructureWithoutPolicy-A countermeasure fully closed across the
  dependency-admission chain AND the SBOM toolchain.

After this seal: V2 carry-forward list is EMPTY.

Plan: docs/plan-qor-phase107-carry-forward-close.md
Audit: .agent/staging/phase107-AUDIT_REPORT.md (PASS, L3 iter-1)
Merkle seal: 5fd157469ffb966e1450a3f1048c5d91771474640c9de79cd30adca5c622c0bb

Authored via [Qor-logic SDLC](github:MythologIQ-Labs-LLC/qor-logic) on [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
