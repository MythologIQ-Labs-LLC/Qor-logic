# AUDIT REPORT

**Target**: `docs/plan-qor-phase291-reconcile-dialect-accessor.md`
**Session**: 2026-09-17T1417-1ab01e
**Branch**: `phase/291-reconcile-dialect-accessor`
**Auditor**: The Qor-logic Judge
**Date**: 2026-09-17

---

## VERDICT: PASS

**Risk Grade**: L1

No violation mandating rejection was found.

## Mode note, stated because the auditor is also the author

`audit_risk_score --plan docs/plan-qor-phase291-reconcile-dialect-accessor.md`
returned `option_b_required: false (no author-momentum risk signal)`.
Independent review was not dispatched for this pass; the justification is
recorded rather than assumed. This is a two-line accessor-routing fix (GH
#477) with a fully specified, mechanical direction and no design surface —
the marginal value of a second reviewer on a change this narrow, already
covered by two red-then-green regression tests, is low.

## Mechanical gates

| Gate | Result |
|---|---|
| `audit_risk_score --plan <plan>` | `option_b_required: false` |
| `plan_text_consistency_lint` | n/a (no `--check` target this plan declares) |
| `plan_test_lint --plan <plan>` | rc=0 |
| `dod_check --plan <plan>` | rc=0, 0 findings (after DoD section added) |
| `plan_iteration_status_lint --plan <plan>` | rc=0 |
| `plan_feature_tdd_lint --plan <plan>` | rc=0 |
| `plan_grep_lint --plan <plan>` | 0 citations declared (plan cites GH #477 and exact line numbers verified by direct read, not `grep -n` evidence syntax) |
| `prompt_injection_canaries --files <plan>` | rc=0 |
| `prose_test_lint --enforce` | rc=0 (69 pre-existing exemptions, none new) |
| Publication boundary scan of the plan | 0 findings (manual read: plan references only GH #477, `ledger_dialect`/`reconcile.py`/`ledger_hash.py` in-repo symbols, and repo-local test paths; no outside-repository identity, path, or credential) |

## Passes

**Security L3 / OWASP.** No auth, credential, secret, or network surface
touched. The change routes two existing regex-match objects through an
already-published, already-tested accessor (`ledger_dialect.hash_value`);
no new input path, no new trust boundary. No violation.

**Ghost UI.** N/A — no UI surface in this plan.

**Section 4 Simplicity Razor.**

| Check | Limit | This change | Status |
|---|---|---|---|
| Max function lines | 40 | `detect_residual` and `_recorded_chain_hash` are unchanged in line count (one line each is a same-length replacement) | OK |
| Max file lines | 250 | `reconcile.py` is 127 lines pre- and post-change | OK |
| Max nesting depth | 3 | Unchanged | OK |
| Nested ternaries | 0 | 0 | OK |

**Dependency audit.** No new dependency. `ledger_dialect` is already an
in-repo module, already imported by the sibling `ledger_hash.py` for the
identical purpose.

**Macro-level architecture.** This closes a layering violation rather than
introducing one: `ledger_dialect` owns the `_HASH_VALUE` capture-group
layout (documented at `ledger_dialect.py`'s `OWNS`/`hash_value` doctrine
comment), and `reconcile.py` was the one remaining consumer bypassing the
published accessor and indexing the owner's private group layout directly.
After this change, `ledger_hash.py` and `reconcile.py` both read the
dialect exclusively through `hash_value`.

**Orphan detection.** No new files. The one changed file
(`qor/scripts/reconcile.py`) is on the existing `reconcile` CLI's import
path (`qor.scripts.reconcile`, wired into `qor-logic reconcile`); the one
changed test file (`tests/test_reconcile.py`) is collected by the existing
pytest suite.

## Test Functionality

Both new tests invoke the unit under test and assert on its return value:

- `test_detect_residual_groups_bare_line_previous_hash_with_backtick_form`
  calls `reconcile.detect_residual(...)` and asserts the returned dict
  groups two entries (one backtick-form, one bare-line-form Previous Hash)
  under the same real hash key, and that `None` is not a key.
- `test_last_chain_hash_reads_bare_line_chain_hash_form` calls
  `reconcile._last_chain_hash(text)` and asserts the return equals the
  independently-computed `chain_hash(...)` for a bare-line-form Chain Hash
  entry.

Both were confirmed **red** against the pre-fix `group(1) or group(2)` code
(the bare-line form resolved to `None` at both call sites, producing a
`None`-keyed group and a raised `ValueError` respectively) and **green**
after the fix. Re-run twice for determinism (`tests/test_reconcile.py`: 10
passed, both runs).

## Verification (re-run fresh for this report, not inherited)

- `python -m pytest tests/test_reconcile.py -q`: 10 passed (x3 runs).
- `python -m pytest tests/ -q`: 3491 passed / 26 skipped / 4 deselected / 1
  failed. The 1 failure
  (`tests/test_plan_grep_evidence_parse.py::test_reproduces_compares_stripped_text`)
  is pre-existing and unrelated: confirmed to fail identically on unmodified
  `origin/main` (`git stash` + re-run) before this branch's changes existed.
  It compares live file content at git ref `2d356ec` against
  `qor/scripts/plan_grep_lint.py:97`, a file this plan does not touch.
- `ruff check qor/ tests/`: all checks passed.
- `python -m qor.scripts.check_variant_drift`: 406 files, no drift.
- `qor-logic verify-ledger`: entries #776-#795 chain-verified clean on the
  branch point before this session's append.

## Disclosed, out of this phase's scope

Running the full suite regenerates `qor/dist/manifest.json` and its variant
copies as an observed side effect of the local test run (different
`generated_ts` and content hashes vs. the committed copies) — not caused by
this phase's own edits (`git stash` reproduces the same regeneration on
unmodified `origin/main`). Discarded (`git checkout -- qor/dist/`) before
staging; not part of this phase's diff.

**Required next action**: /qor-implement (already executed; proceeding to
/qor-substantiate).
